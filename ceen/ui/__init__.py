"""The window: screens, reusable widgets and the theme they share.

    theme.py        colors, fonts, color-blending helpers
    widgets/        buttons, toggles, ring, server list, results table
    panels/         the two full screens (Connect + Checker)
    app.py          the controller that wires screens to core/
"""
