"""Shared helpers used by both the brain (core) and the window (ui).

    netconfig.py  Windows system-proxy switch + the kill-switch
    textutil.py   small formatting helpers (bytes, country flags, names)
"""
