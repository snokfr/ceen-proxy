"""Everything about a single proxy: what it is and how to talk to it.

    entry.py       the ProxyEntry object — one row in your proxy list
    handshakes.py  the raw conversations (HTTP-CONNECT / SOCKS4 / SOCKS5)
    checker.py     the full "does this proxy actually work?" test
"""
